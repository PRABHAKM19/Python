def act(s,f):
    a=len(f)
    i=0
    print(i,end=" ")
    for j in range(1,a):
        if s[j]>=f[i]:
            print(j,end=" ")
        i=j
s=list(map(int,input().split()))
f=list(map(int,input().split()))
act(s,f)
